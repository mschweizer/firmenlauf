"""Application configuration for the runs app."""

from django.apps import AppConfig


class RunsConfig(AppConfig):
    """Configuration class for the runs application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "runs"
